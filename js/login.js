/**
 * Created by Adrian Lundhe, on 2017-04-10, Solna.
 *
 * Loads the pop-up menu to the user.
 */

/**
 * Load the pop-up menu to the user.
 */
$(window).load(login = function(){
    $('#popUpWindow').modal('show');
});